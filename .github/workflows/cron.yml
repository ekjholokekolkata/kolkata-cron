name: Central Notifier Cron

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  run-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Central Notifier
        run: |
          echo "Calling InfinityFree cron..."

          curl -L --fail --show-error --max-time 30 \
          "https://ekjholokekolkata.page.gd/cron.php?token=YOUR_SECRET_TOKEN"

          echo "Done."
