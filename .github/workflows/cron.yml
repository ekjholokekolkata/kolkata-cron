name: Central Notifier Cron

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  run-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Central Notifier
        run: |
          echo "Calling InfinityFree cron..."

          curl -i -L --fail --show-error --max-time 30 \
          "https://ekjholokekolkata.page.gd/cron.php?token=CHANGE_THIS_TO_STRONG_RANDOM_STRING"

          echo "Done."
