name: Central Notifier Cron

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  run_cron:
    runs-on: ubuntu-latest

    steps:
      - name: Call Central Notifier
        run: |
          echo "Running central notifier..."
          curl -L "https://ekjholokekolkata.page.gd.com/central_notifier.php"
          echo "Done."
